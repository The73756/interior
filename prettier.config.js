const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  quoteProps: 'consistent',
  printWidth: 100,
  trailingComma: 'none'
}

export default config
